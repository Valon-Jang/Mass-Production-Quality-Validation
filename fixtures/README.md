# Fixtures

Only synthetic, non-sensitive workbooks belong here. Actual user OQC files and
the immutable planning ZIP are never copied into automated test fixtures.
Golden acceptance data will be added only with explicit handling rules and its
source hash.

